{% extends "layout.html.twig" %}
{% block contenu %}
    <div class="row">
        {% if   app.session.get('droit') is defined and  app.session.get('droit') == 'DROITadmin' %}
        <table>
            <caption>Les utilisateurs (avec twig)</caption>
            <thead>
            <tr><th>login</th>><th>Supprimer</th>
                {% endif %}
            </tr>
            </thead>
            <tbody>
            {% for user in data if data is not empty %}

                <tr>
                    <td>{{user.login}}</td><td><a href="{{ path('user.validFormDelete', {id: user.id}) }}"> <img src={{asset('images/boutonsuppr.jpg')}} style="width:50px"  > </a></td>
                </tr>
            {% endfor %}
            <tbody>
        </table>
    </div>
{% endblock %}