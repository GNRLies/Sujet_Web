{% extends "layout.html.twig" %}
{% block contenu %}
	<div class="row">
		{% if   app.session.get('droit') is defined and  app.session.get('droit') == 'DROITadmin' %}

		{% endif %}

		<table>
			<caption>Recapitulatifs des produits (avec twig)</caption>
			<thead>
			<tr><th>id</th><th>quantite</th><th>prix</th><th>dateAjoutPanier</th><th>user_id</th><th>jeux_id</th><th>commande_id</th>
				{% if   app.session.get('droit') is defined and  app.session.get('droit') == 'DROITadmin' %}
					<th>opération</th>
				{% endif %}
			</tr>
			</thead>
			<tbody>
			{% for Paniers in data if data is not empty %}

				<tr>
					<td>{{Panier.id}}</td><td>{{Panier.quantite}}</td><td>{{Panier.prix}}</td><td>{{Panier.dateAjoutPanier}}</td><td>
						{{Panier.user_id}}</td><td> {{Panier.jeux_id}}
					</td><td> {{Panier.commande_id}}
					</td>
					{% if   app.session.get('droit') is defined and  app.session.get('droit') == 'DROITadmin' %}
						<td>
						</td>
					{% endif %}
				</tr>
			{% endfor %}
			<tbody>
		</table>
	</div>
{% endblock %}

